'use strict';

// Register `homeModule` component, along with its associated template. This module does not need a controller.
angular.
  module('homeModule').
  component('homeModule', {
    templateUrl: 'home-page/home-page.template.html'
  });
